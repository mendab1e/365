# frozen_string_literal: true

# This project intentionally has no application server.
# Serve the generated public/ directory with any static file host instead.
raise "No application server: serve ./public as static files"
