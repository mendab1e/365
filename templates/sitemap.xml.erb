<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
  <url>
    <loc><%= h(absolute("")) %></loc>
  </url>
  <url>
    <loc><%= h(absolute("about.html")) %></loc>
  </url>
  <% photos.reverse_each do |photo| %>
  <url>
    <loc><%= h(absolute("posts/#{photo.fetch(:date)}.html")) %></loc>
    <image:image>
      <image:loc><%= h(absolute(photo.fetch(:image))) %></image:loc>
    </image:image>
  </url>
  <% end %>
</urlset>
