<article class="photo-post single-photo">
  <h1><%= long_date(@photo) %></h1>
  <%= render("photo", photo: @photo, deferred: false) %>
  <p class="back-link"><a href="<%= public_path("index.html") %>">← Back to all photos</a></p>
</article>
